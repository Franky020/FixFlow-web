:80 {
    # Raíz del sitio (apunta a la carpeta public de Laravel)
    root * /var/www/html/public

    # Habilitar manejo de archivos estáticos
    file_server

    # Conexión a PHP-FPM a través del puerto 9000 (CORRECTO)
    php_fastcgi 127.0.0.1:9000

    # Opcional pero útil para depuración
    # log {
    #     output stdout
    # }
}
